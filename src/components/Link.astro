---
interface Props {
  href: string;
  target?: string;
  label: string;
}

const { href, label } = Astro.props;
const target = Astro.props.target || "_blank";
---

<a href={href} target={target}>{label}</a>

<style>
  a {
    text-decoration: underline;
    font-weight: 500;
  }
</style>
